{
  "name": "@narrativegen/web-tester",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prebuild": "node -e \"const fs=require('fs');['dist','node_modules/.vite'].forEach(p=>{if(fs.existsSync(p))fs.rmSync(p,{recursive:true})})\"",
    "build": "vite build",
    "postbuild": "",
    "copy:models": "node copy-models.js",
    "dev": "vite",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed"
  },
  "dependencies": {
    "@narrativegen/engine-ts": "file:../../packages/engine-ts"
  },
  "devDependencies": {
    "vite": "^5.4.9",
    "@playwright/test": "^1.42.0"
  }
}
